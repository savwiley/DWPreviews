import styled from "styled-components";

export const Head = styled.div `
  font: 150px var(--headerFont);
  color: var(--header);
  cursor: default;
  letter-spacing: 4px;
  width: 50%;
  padding: 20px;
  word-break: break-all;
`;